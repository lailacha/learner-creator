/:
  controller: main
  action: home




/nous-contacter:
  controller: main
  action: contact

/login:
  controller: user
  action: login

/logout:
  controller: user
  action: logout

/register:
  controller: user
  action: register

/recover-password:
  controller: user
  action: recoverPassword


/change-password:
  controller: user
  action: changePassword

/dashboard:
  controller: admin
  action: home
  security: true

/mail:
  controller: user
  action: sendRegisterMail

/verifyAccount:
  controller: user
  action: verifyAccount
